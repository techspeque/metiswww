---
/**
 * Install — placeholder section for the Phase-0 skeleton.
 * Phase 1 fills this with the install steps and outbound links. Styling
 * consumes tokens only.
 */
---

<section id="install" class="section">
  <h2>Install</h2>
  <p>Placeholder for the install steps and links.</p>
</section>

<style>
  .section {
    padding-block: var(--space-8);
    border-bottom: 1px solid var(--color-border);
  }

  .section p {
    margin-block-start: var(--space-3);
    color: var(--color-muted);
  }
</style>
