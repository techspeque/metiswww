---
/**
 * Protocol — placeholder section for the Phase-0 skeleton.
 * Phase 1 fills this with the animated slice-lifecycle diagram. Styling
 * consumes tokens only.
 */
---

<section id="protocol" class="section">
  <h2>The protocol</h2>
  <p>Placeholder for the slice-lifecycle diagram.</p>
</section>

<style>
  .section {
    padding-block: var(--space-8);
    border-bottom: 1px solid var(--color-border);
  }

  .section p {
    margin-block-start: var(--space-3);
    color: var(--color-muted);
  }
</style>
