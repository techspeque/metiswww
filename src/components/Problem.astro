---
/**
 * Problem — placeholder section for the Phase-0 skeleton.
 * Phase 1 fills this with the problem/insight, condensed from the metis
 * README. Styling consumes tokens only.
 */
---

<section id="problem" class="section">
  <h2>The problem</h2>
  <p>Placeholder for the problem and insight behind metis.</p>
</section>

<style>
  .section {
    padding-block: var(--space-8);
    border-bottom: 1px solid var(--color-border);
  }

  .section p {
    margin-block-start: var(--space-3);
    color: var(--color-muted);
  }
</style>
