---
/**
 * Hero — placeholder section for the Phase-0 skeleton.
 * Phase 1 replaces this with the typed-terminal animation and install
 * one-liner. Carries the page's single h1. Styling consumes tokens only.
 */
---

<section id="hero" class="section hero">
  <h1>metis</h1>
  <p>The meta-harness that keeps AI coding agents on task.</p>
</section>

<style>
  .section {
    padding-block: var(--space-8);
    border-bottom: 1px solid var(--color-border);
  }

  .hero p {
    margin-block-start: var(--space-3);
    font-size: var(--text-lg);
    color: var(--color-muted);
  }
</style>
